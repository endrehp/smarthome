<html>
    <head>    

    </head>
    <body>

        <button onclick="closeSSE()">Close SSE-connection</button>
        <script>
            const sseSource = new EventSource('/event-stream');

            sseSource.addEventListener('message', (e) => {
                const messageData = e.data;
                console.log("From server: " + messageData);
            });

            // When finished with the source close the connection
            function closeSSE() {
                sseSource.close();
            }
            
            </script>
        <!--
        <script src="/js/modeEditor_script.js"></script>
        -->
    </body>


</html>